<svg class="sorein-icon{% if class != blank %} {{ class }}{% endif %}"
     width="{{ width | default: 20 }}"
     height="{{ height | default: 20 }}"
     viewBox="0 0 24 24"
     aria-hidden="true"
     focusable="false">
  <use href="#icon-{{ icon }}"></use>
</svg>
